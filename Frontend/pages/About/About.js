import React, { useEffect, useState } from "react";
import { Link, useLocation } from "react-router-dom";
import Breadcrumbs from "../../components/pageProps/Breadcrumbs";

const About = () => {
  const location = useLocation();
  const [prevLocation, setPrevLocation] = useState("");
  useEffect(() => {
    setPrevLocation(location.state.data);
  }, [location]);
  return (
    <div className="max-w-container mx-auto px-4" style={{ backgroundColor: '#D5F0C1' }}>
      <Breadcrumbs title="About" prevLocation={prevLocation} />
      <div className="pb-10">
        <h1 className="max-w-[600px] text-base text-lightText mb-2">
          <span className="text-primeColor font-semibold text-lg">Delightful Delivers</span>{" "}Welcomes you, where every gift tells a story and every delivery brings joy. Our mission is to make every occasion memorable with handpicked, high-quality gifts that convey your sentiments perfectly. Whether you’re celebrating a birthday, anniversary, holiday, or just want to brighten someone’s day, we have the ideal present waiting for you.
          
        </h1>
        

        
        <Link to="/shop">
          <button className="w-52 h-10 bg-primeColor text-white hover:bg-black duration-300">
            Continue Shopping
          </button>
        </Link>
      </div>
    </div>
  );
};

export default About;
